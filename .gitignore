# 빌드 원본 — minified 버전만 배포 (public repo에는 풀소스 비포함)
globe-engine.js
app.js
iir-db.js
translations.js

# 테스트 코드 (엔진 동작 노출 방지)
test/

# Node modules (esbuild 등 빌드 도구는 dev-only, push에 포함 안 함)
node_modules/

# 참고 자료 (jurisdictions 데이터 출처 PDF — 로컬 전용)
*.pdf

# Python cache
__pycache__/
scripts/__pycache__/
